$(document).ready(function() {
  $('#fullpage').fullpage({
		// fixedElements: '.header',
		anchors : ['home', 'pages', 'contact'],
		sectionsColor : ['#000', '#000', '#fff'],
		controlArrows : false
  });
});